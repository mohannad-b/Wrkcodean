import AutomationDetailPageClient, { type AutomationDetailPageProps } from "./AutomationDetailPageClient";

export default function Page(props: AutomationDetailPageProps) {
  return <AutomationDetailPageClient {...props} />;
}

